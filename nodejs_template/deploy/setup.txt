# 1. Create project folders
mkdir -p nodejs_image_pipeline/src nodejs_image_pipeline/deploy
cd nodejs_image_pipeline

# 2. Create all source files in src/
# (config.js, rotate.js, zoom.js, greyscale.js, Inspector.js)

# 3. Create zip.sh in deploy/

# 4. Install dependencies
cd deploy
npm init -y
npm install @aws-sdk/client-s3 uuid@3
npm install --os=linux --cpu=x64 sharp

# 5. Make script executable
chmod +x zip.sh

# 6. Edit src/config.js with your bucket names

# 7. Create zip files
./zip.sh

# 8. Upload zips to Lambda in AWS Console
